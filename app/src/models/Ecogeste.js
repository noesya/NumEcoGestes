export default class Ecogeste {
    constructor ({ title, thanks, description, sources, tag, points, credit }) {
        this.title = title;
        this.thanks = thanks;
        this.description = description;
        this.sources = sources;
        this.tag = tag;
        this.points = points;
        this.credit = credit;
    }
}